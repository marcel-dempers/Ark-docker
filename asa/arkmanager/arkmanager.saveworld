#!/bin/bash

instance=$1

if [ -z "$instance" ]
then
      echo "must supply instance, e.g @island"
      exit
fi

instance="${instance:1}"

echo "saving world on instance ${instance} ..."
arkmanager rcon "@${instance}" saveworld